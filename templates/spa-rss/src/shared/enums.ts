export enum StoreKey {
  Main = 'main',
}

export enum Action {
  UpdateFirst = 'update first',
  UpdateSecond = 'update second',
}

export enum BtnText {
  IncFirst = 'inc first',
  IncSecond = 'inc second',
  Reset = 'reset',
}
